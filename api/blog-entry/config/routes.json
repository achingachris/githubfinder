{
  "routes": [
    {
      "method": "GET",
      "path": "/blog-entries",
      "handler": "blog-entry.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/blog-entries/count",
      "handler": "blog-entry.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/blog-entries/:id",
      "handler": "blog-entry.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/blog-entries",
      "handler": "blog-entry.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/blog-entries/:id",
      "handler": "blog-entry.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/blog-entries/:id",
      "handler": "blog-entry.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
